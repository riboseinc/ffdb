source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

# Specify your gem's dependencies in ffdb.gemspec
gemspec

group :test do
  gem "rubocop", require: false
end
